---
phase: 2
plan: 1
title: "SelectionScreen Canvas & Scene Roots"
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Billboard.cs
requirements:
  - SEL-01
  - SEL-02
  - SEL-03
  - SEL-06
autonomous: true
---

# Plan 02-01: SelectionScreen Canvas & Scene Roots

<objective>
Create the SelectionScreen World Space Canvas with two scenario cards (KitchenCard, ServerCard), attach Billboard.cs for player-facing behavior, and create inactive Kitchen/ServerRoom root GameObjects in the scene hierarchy.
</objective>

<must_haves>
- SelectionScreen World Space Canvas at (0, 1.5, 3) with correct scale
- KitchenCard panel with orange color and TMP_Text
- ServerCard panel with blue color and TMP_Text
- Billboard.cs script attached to SelectionScreen
- Kitchen and ServerRoom empty GameObjects set inactive
</must_haves>

## Tasks

<task id="02-01-01">
<title>Create Billboard.cs script</title>

<read_first>
- Assets/Scripts/PlayerMovement.cs (existing script pattern, namespace/using conventions)
- .planning/phases/02-scenario-selection-screen/02-CONTEXT.md (D-09 through D-11 — exact script)
</read_first>

<action>
Create `Assets/Scripts/Billboard.cs` with the following exact content:

```csharp
using UnityEngine;

public class Billboard : MonoBehaviour
{
    void Update()
    {
        if (Camera.main != null)
            transform.LookAt(Camera.main.transform);
    }
}
```

This script is user-specified verbatim — do not modify.
</action>

<acceptance_criteria>
- File `Assets/Scripts/Billboard.cs` exists
- File contains `public class Billboard : MonoBehaviour`
- File contains `transform.LookAt(Camera.main.transform)`
- File contains null check `if (Camera.main != null)`
- File uses only `using UnityEngine;` — no InputSystem import needed
</acceptance_criteria>
</task>

<task id="02-01-02">
<title>Create SelectionScreen World Space Canvas in scene</title>

<read_first>
- .planning/phases/02-scenario-selection-screen/02-CONTEXT.md (D-01 through D-08 — canvas and card specs)
</read_first>

<action>
In the Unity scene, create the following hierarchy:

1. **SelectionScreen** (GameObject with Canvas component)
   - Canvas Render Mode: World Space
   - Position: (0, 1.5, 3)
   - RectTransform: Width=400, Height=200
   - Scale: (0.005, 0.005, 0.005)
   - Add a Canvas Scaler component (if not auto-added)
   - Attach `Billboard.cs` script

2. **KitchenCard** (child of SelectionScreen — Panel/Image)
   - RectTransform anchored position: (-80, 0)
   - Image color: #E8632A (orange)
   - Child **TMP_Text**: text="Kitchen Fire\nPress A", color=white, fontSize=24, alignment=center

3. **ServerCard** (child of SelectionScreen — Panel/Image)
   - RectTransform anchored position: (+80, 0)
   - Image color: #2A6BE8 (blue)
   - Child **TMP_Text**: text="Server Room Fire\nPress B", color=white, fontSize=24, alignment=center

Since this is a Unity scene setup, the executor must perform these operations via Unity Editor scripting or manual setup instructions. The key values are exact and must match.
</action>

<acceptance_criteria>
- Scene hierarchy contains GameObject "SelectionScreen" with Canvas component in World Space mode
- SelectionScreen position is (0, 1.5, 3) with scale (0.005, 0.005, 0.005)
- SelectionScreen RectTransform width=400, height=200
- Billboard script component is attached to SelectionScreen
- KitchenCard child Panel exists at anchored position (-80, 0) with Image color #E8632A
- KitchenCard has child TMP_Text with text "Kitchen Fire\nPress A", white, size 24
- ServerCard child Panel exists at anchored position (80, 0) with Image color #2A6BE8
- ServerCard has child TMP_Text with text "Server Room Fire\nPress B", white, size 24
</acceptance_criteria>
</task>

<task id="02-01-03">
<title>Create inactive Kitchen and ServerRoom root objects</title>

<read_first>
- .planning/phases/02-scenario-selection-screen/02-CONTEXT.md (D-20, D-21 — inactive root objects)
</read_first>

<action>
In the Unity scene:

1. Create empty GameObject named **"Kitchen"** at position (0, 0, 0)
   - Set active = **false** (uncheck the checkbox at top of Inspector)
   - This will be the parent for all kitchen environment geometry in Phase 3

2. Create empty GameObject named **"ServerRoom"** at position (0, 0, 0)
   - Set active = **false** (uncheck the checkbox at top of Inspector)
   - This will be the parent for all server room geometry in Phase 4

Both objects must be at root level in the scene hierarchy (not children of anything).
</action>

<acceptance_criteria>
- Scene contains root-level GameObject "Kitchen" with activeSelf = false
- Scene contains root-level GameObject "ServerRoom" with activeSelf = false
- Both are empty GameObjects (no components beyond Transform)
- Both are at position (0, 0, 0)
</acceptance_criteria>
</task>

## Verification

```
VERIFY:
  - Assets/Scripts/Billboard.cs exists and compiles
  - SelectionScreen canvas visible in scene at correct position
  - Both cards visible with correct colors and text
  - Kitchen and ServerRoom objects exist and are inactive
  - No compile errors in Unity Console
```
