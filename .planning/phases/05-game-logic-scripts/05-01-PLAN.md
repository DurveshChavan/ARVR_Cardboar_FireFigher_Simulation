---
wave: 1
depends_on: []
files_modified: [
  "Assets/Scripts/GameManager.cs",
  "Assets/Scripts/ExtinguisherController.cs",
  "Assets/Scripts/SprayController.cs",
  "Assets/Scripts/FireController.cs"
]
autonomous: true
requirements_addressed: [LOGIC-01]
---

# 05-01: Game Logic Scripts

<objective>
Generate the core game logic scripts exactly as specified, handling the timer, input, extinction checking, and state management.
</objective>

<tasks>

  <task>
    <description>Create GameManager</description>
    <read_first>
      - .planning/phases/05-game-logic-scripts/05-01-CONTEXT.md
    </read_first>
    <action>
      Write `Assets/Scripts/GameManager.cs` implementing the timer countdown, scenario awareness, static Instance, and UI updating hooks using TMPro.
    </action>
    <acceptance_criteria>
      - `GameManager.cs` exists and compiles.
    </acceptance_criteria>
  </task>

  <task>
    <description>Create ExtinguisherController</description>
    <read_first>
      - .planning/phases/05-game-logic-scripts/05-01-CONTEXT.md
    </read_first>
    <action>
      Write `Assets/Scripts/ExtinguisherController.cs` tracking Y-button (gamepad.buttonNorth) presses to cycle DCP/CO2/Water and showing a popup.
    </action>
    <acceptance_criteria>
      - `ExtinguisherController.cs` exists and compiles.
    </acceptance_criteria>
  </task>

  <task>
    <description>Create SprayController</description>
    <read_first>
      - .planning/phases/05-game-logic-scripts/05-01-CONTEXT.md
    </read_first>
    <action>
      Write `Assets/Scripts/SprayController.cs` detecting LT+RT trigger pulls, finding the current fire controller via GameManager, checking effectiveness logic, and reducing the fire's scale. Includ es instant fail condition for Water on Electrical.
    </action>
    <acceptance_criteria>
      - `SprayController.cs` exists and compiles.
    </acceptance_criteria>
  </task>

  <task>
    <description>Update FireController</description>
    <read_first>
      - .planning/phases/05-game-logic-scripts/05-01-CONTEXT.md
    </read_first>
    <action>
      Overwrite any existing `Assets/Scripts/FireController.cs` stub with the full logic scaling down based on `fireScale` and firing `MissionComplete` at <= 0.05.
    </action>
    <acceptance_criteria>
      - `FireController.cs` is fully implemented.
    </acceptance_criteria>
  </task>

  <task>
    <description>Automate Scene Assembly & Linkages</description>
    <read_first>
      - .planning/phases/05-game-logic-scripts/05-01-CONTEXT.md
    </read_first>
    <action>
      Create an Editor script (`Assets/Editor/GameLogicWirer.cs`) that attaches `GameManager` to an empty GameObject, attaches the controllers to `PlayerRig`, attaches `FireController` to the two environment fire particle systems, and wires missing Inspector dependencies programmatically.
    </action>
    <acceptance_criteria>
      - An Editor script is provided to wire scripts, removing manual drag-and-drop burden from the player.
    </acceptance_criteria>
  </task>

</tasks>

<verification>
### Automated Verification
None (compilation validation occurs inherently in Unity).

### Manual Verification
1. Open the project in Unity to confirm no red compiler errors in console.
2. Run the Editor script `VR Firefighter > Wire Component Logic` and ensure all connections are satisfied.
</verification>

<must_haves>
- Uses New Input System exclusively (`Gamepad.current`).
- C# exactly matching the user's provided specs.
</must_haves>
