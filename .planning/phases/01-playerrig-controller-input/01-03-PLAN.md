---
phase: 01-playerrig-controller-input
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified: []
autonomous: false
requirements: [RIG-03, RIG-04, RIG-05, RIG-06, RIG-07]

must_haves:
  truths:
    - "Left stick X/Y on Xbox controller moves PlayerRig in scene view"
    - "PlayerRig does not fall through the test floor (gravity + grounding works)"
    - "No console errors appear during Play mode"
    - "No legacy Input API calls exist anywhere in the project"
    - "Camera does not have any rotation scripts"
  artifacts: []
  key_links:
    - from: "Xbox controller left stick"
      to: "PlayerRig movement"
      via: "Gamepad.current.leftStick → CharacterController.Move()"
---

<objective>
Verify Xbox controller input works in Unity Play mode: left stick moves PlayerRig, no console errors, no legacy Input API usage. Per user verification criteria D-19, D-20.

Purpose: Confirm the full PlayerRig + Controller Input chain works end-to-end before proceeding to Phase 2.
Output: Verified working input, clean console.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-playerrig-controller-input/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify no legacy Input API in codebase</name>
  <files></files>
  <read_first>
    - VR_Firefighter/Assets/Scripts/PlayerMovement.cs
  </read_first>
  <action>
Search the entire `VR_Firefighter/Assets/Scripts/` directory for any legacy Input API usage:

```bash
grep -rn "Input.GetAxis\|Input.GetButton\|Input.GetButtonDown\|Input.GetKey\|Input.mousePosition" VR_Firefighter/Assets/Scripts/ 2>/dev/null
```

Expected result: NO matches. If any are found, they must be removed and replaced with New Input System equivalents.

Also verify PlayerMovement.cs contains the required null check:
```bash
grep "Gamepad.current" VR_Firefighter/Assets/Scripts/PlayerMovement.cs
grep "if (gp == null) return" VR_Firefighter/Assets/Scripts/PlayerMovement.cs
```

Both should return matches (per RIG-05, RIG-06).
  </action>
  <acceptance_criteria>
    - `grep -rn "Input.GetAxis\|Input.GetButton" VR_Firefighter/Assets/Scripts/` returns 0 results
    - `grep "Gamepad.current" VR_Firefighter/Assets/Scripts/PlayerMovement.cs` returns 1 result
    - `grep "gp == null" VR_Firefighter/Assets/Scripts/PlayerMovement.cs` returns 1 result
  </acceptance_criteria>
  <verify>
    grep -c "Input.GetAxis\|Input.GetButton\|Input.GetButtonDown" VR_Firefighter/Assets/Scripts/PlayerMovement.cs returns 0
  </verify>
  <done>No legacy Input API found in codebase; Gamepad.current with null check confirmed present</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Xbox controller input in Play mode</name>
  <what-built>PlayerRig with CharacterController movement driven by Xbox controller left stick via New Input System</what-built>
  <how-to-verify>
1. Connect Xbox controller to PC via Bluetooth or USB
2. Open Unity Editor with the VR_Firefighter project
3. Press Play button to enter Play mode
4. Push LEFT STICK in any direction on the Xbox controller
5. Watch the Scene View (not Game View) — PlayerRig should move:
   - Left stick LEFT → PlayerRig strafes left
   - Left stick RIGHT → PlayerRig strafes right
   - Left stick UP → PlayerRig moves forward
   - Left stick DOWN → PlayerRig moves backward
6. Check the Console window — should show NO errors (yellow warnings are OK)
7. Verify PlayerRig stays on the test floor (doesn't fall through — gravity + grounding working)
8. Press Play again to exit Play mode

**If no Xbox controller is connected:**
- PlayerRig should NOT move (Gamepad.current == null, safe return)
- Console should show NO errors
  </how-to-verify>
  <resume-signal>Type "approved" if left stick moves PlayerRig and no console errors. Otherwise describe the issue.</resume-signal>
  <done>Xbox controller left stick moves PlayerRig in Play mode with no console errors</done>
</task>

</tasks>

<verification>
1. No legacy Input API (Input.GetAxis, etc.) anywhere in Assets/Scripts/
2. Gamepad.current null check present
3. Left stick moves PlayerRig in Play mode (user verified)
4. No console errors during Play mode
5. PlayerRig stays grounded on test floor
</verification>

<success_criteria>
- Left stick on Xbox controller moves PlayerRig forward/back/strafe at speed 3f
- Gravity keeps PlayerRig grounded on test floor
- Gamepad.current null check prevents errors when no controller connected
- Console is clean — no errors
- No legacy Input API exists in any script
</success_criteria>

<output>
After completion, create `.planning/phases/01-playerrig-controller-input/01-03-SUMMARY.md`
</output>
