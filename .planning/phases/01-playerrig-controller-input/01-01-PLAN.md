---
phase: 01-playerrig-controller-input
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Assets/Scripts/PlayerMovement.cs]
autonomous: true
requirements: [RIG-01, RIG-02, RIG-03, RIG-04, RIG-05, RIG-06, RIG-07]

must_haves:
  truths:
    - "PlayerRig empty GameObject exists at world origin (0,0,0)"
    - "CharacterController component on PlayerRig with height=1.8, radius=0.3, center=(0,0.9,0)"
    - "Main Camera is child of PlayerRig at local position (0,1.6,0) with tag MainCamera"
    - "No MouseLook, CameraController, or rotation scripts on Main Camera"
    - "PlayerMovement.cs attached to PlayerRig with speed=3f"
    - "Gamepad.current null check present in Update()"
    - "Left stick X/Y moves PlayerRig via CharacterController with gravity"
  artifacts:
    - path: "Assets/Scripts/PlayerMovement.cs"
      provides: "Xbox controller movement via New Input System"
      contains: "Gamepad.current"
      min_lines: 20
  key_links:
    - from: "PlayerMovement.cs"
      to: "CharacterController"
      via: "GetComponent<CharacterController>()"
      pattern: "GetComponent<CharacterController>"
    - from: "PlayerMovement.cs"
      to: "Gamepad.current"
      via: "leftStick.x/y.ReadValue()"
      pattern: "Gamepad\\.current"
---

<objective>
Create PlayerRig hierarchy, Main Camera child, PlayerMovement.cs script, and temporary test floor per user-locked decisions D-01 through D-18.

Purpose: Establish the foundational player rig with CharacterController movement driven by Xbox controller left stick via New Input System. Scene includes a test floor plane and directional light.
Output: PlayerMovement.cs script, scene hierarchy ready for Play mode testing.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-playerrig-controller-input/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PlayerMovement.cs script</name>
  <files>Assets/Scripts/PlayerMovement.cs</files>
  <read_first>
    - .planning/phases/01-playerrig-controller-input/01-CONTEXT.md (exact script code in D-15)
    - VR_Firefighter/Packages/manifest.json (confirm com.unity.inputsystem is present)
  </read_first>
  <action>
Create the directory `VR_Firefighter/Assets/Scripts/` if it doesn't exist.

Create `VR_Firefighter/Assets/Scripts/PlayerMovement.cs` with the EXACT code from CONTEXT.md D-15 (user-locked, verbatim):

```csharp
using UnityEngine;
using UnityEngine.InputSystem;

[RequireComponent(typeof(CharacterController))]
public class PlayerMovement : MonoBehaviour
{
    public float speed = 3f;
    private CharacterController cc;
    private float verticalVelocity = 0f;

    void Start()
    {
        cc = GetComponent<CharacterController>();
    }

    void Update()
    {
        var gp = Gamepad.current;
        if (gp == null) return;

        float h = gp.leftStick.x.ReadValue();
        float v = gp.leftStick.y.ReadValue();

        Vector3 move = transform.right * h + transform.forward * v;
        move *= speed;

        verticalVelocity += Physics.gravity.y * Time.deltaTime;
        move.y = verticalVelocity;

        cc.Move(move * Time.deltaTime);

        if (cc.isGrounded) verticalVelocity = 0f;
    }
}
```

This script:
- Uses ONLY `UnityEngine.InputSystem` (per D-10, RIG-05)
- Checks `if (gp == null) return;` (per D-10, RIG-06)
- Reads left stick X/Y for horizontal/vertical movement (per D-11, RIG-03)
- Applies gravity via `Physics.gravity.y * Time.deltaTime` (per D-12, RIG-04)
- Uses `CharacterController.Move()` (per D-13)
- Resets verticalVelocity when grounded (per D-14)
- Speed defaults to 3f (per D-09, RIG-03)

DO NOT modify the script in any way. The user provided it verbatim.
  </action>
  <acceptance_criteria>
    - File exists at `VR_Firefighter/Assets/Scripts/PlayerMovement.cs`
    - File contains `using UnityEngine.InputSystem;`
    - File contains `[RequireComponent(typeof(CharacterController))]`
    - File contains `if (gp == null) return;`
    - File contains `gp.leftStick.x.ReadValue()`
    - File contains `gp.leftStick.y.ReadValue()`
    - File contains `public float speed = 3f;`
    - File contains `cc.Move(move * Time.deltaTime);`
    - File contains `Physics.gravity.y * Time.deltaTime`
    - File does NOT contain `Input.GetAxis` or `Input.GetButton` or `Input.GetButtonDown`
  </acceptance_criteria>
  <verify>
    grep -c "Gamepad.current" VR_Firefighter/Assets/Scripts/PlayerMovement.cs returns 1
    grep -c "Input.GetAxis\|Input.GetButton" VR_Firefighter/Assets/Scripts/PlayerMovement.cs returns 0
  </verify>
  <done>PlayerMovement.cs exists with exact user-specified code, uses only New Input System, has Gamepad null check</done>
</task>

<task type="auto">
  <name>Task 2: Document scene hierarchy setup instructions</name>
  <files>Assets/Scripts/PlayerMovement.cs</files>
  <read_first>
    - .planning/phases/01-playerrig-controller-input/01-CONTEXT.md (D-01 through D-18 for exact hierarchy)
  </read_first>
  <action>
Since Unity scene setup (GameObjects, Components, hierarchy) cannot be done via script files alone and requires the Unity Editor, document the EXACT scene setup steps that must be performed in the Unity Editor. This is a Unity project — scene hierarchy is configured in the Editor, not via code.

The user will need to perform these steps in Unity Editor (or they will be verified in the verification plan):

**PlayerRig Setup (per D-01, D-02, D-03):**
1. Create empty GameObject named "PlayerRig" at position (0, 0, 0)
2. Add CharacterController component: height=1.8, radius=0.3, center=(0, 0.9, 0)  
3. Add PlayerMovement.cs script component

**Main Camera Setup (per D-04, D-05, D-06, D-07):**
1. Delete the default "Main Camera" from scene root
2. Create new Camera as CHILD of PlayerRig
3. Set local position to (0, 1.6, 0)
4. Set tag to "MainCamera"
5. DO NOT add any MouseLook, CameraController, or rotation scripts

**Scene Objects (per D-16, D-17, D-18):**
1. Keep the default Directional Light unchanged
2. Create a Plane at (0, 0, 0) with scale (5, 1, 5) as temporary test floor
3. No other objects needed

No code changes in this task — the PlayerMovement.cs was created in Task 1. This task documents what must be done in Unity Editor. The verification plan (Plan 03) will check these are set up correctly.
  </action>
  <acceptance_criteria>
    - PlayerMovement.cs script exists (created in Task 1)
    - Scene setup instructions are clear and match CONTEXT.md decisions D-01 through D-18
  </acceptance_criteria>
  <verify>
    File VR_Firefighter/Assets/Scripts/PlayerMovement.cs exists
  </verify>
  <done>Scene hierarchy instructions documented; PlayerMovement.cs ready for attachment in Editor</done>
</task>

</tasks>

<verification>
1. `VR_Firefighter/Assets/Scripts/PlayerMovement.cs` exists with correct content
2. Script uses only `UnityEngine.InputSystem` — no legacy Input API
3. `Gamepad.current` null check is present in Update()
4. `speed = 3f` default is set
5. CharacterController movement with gravity is implemented
</verification>

<success_criteria>
- PlayerMovement.cs matches user-provided code exactly
- No references to legacy Input API anywhere in the script
- Gamepad.current null safety check present
- Script is ready to be attached to PlayerRig in Unity Editor
</success_criteria>

<output>
After completion, create `.planning/phases/01-playerrig-controller-input/01-01-SUMMARY.md`
</output>
