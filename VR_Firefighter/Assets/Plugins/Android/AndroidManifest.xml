<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.unity3d.player"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions required by Cardboard SDK -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.NFC"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>

    <!-- Vibration for controller haptics -->
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!-- OpenGL ES 3.0 required by Cardboard -->
    <uses-feature android:glEsVersion="0x00030000" android:required="true"/>

    <!-- Camera required for QR code scanner -->
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/app_icon"
        tools:replace="android:label">

        <!--
            CardboardUnityPlayerActivity wraps UnityPlayerActivity and pre-loads
            libGfxPluginCardboard.so using the APPLICATION classloader, so that
            JNI_OnLoad can resolve com.google.cardboard.sdk.qrcode.* via FindClass().
            Without this, RegisterNatives crashes with java_class == null (SIGABRT).
        -->
        <activity
            android:name="com.unity3d.player.CardboardUnityPlayerActivity"
            android:label="@string/app_name"
            android:screenOrientation="fullSensor"
            android:launchMode="singleTask"
            android:configChanges="mcc|mnc|locale|touchscreen|keyboard|keyboardHidden|navigation|orientation|screenLayout|uiMode|screenSize|smallestScreenSize|fontScale|layoutDirection|density"
            android:hardwareAccelerated="false"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Cardboard QR code scanner activity (from the Cardboard AAR) -->
        <activity
            android:name="com.google.cardboard.sdk.QrCodeCaptureActivity"
            android:exported="false"
            android:label="Cardboard QR Scanner"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>

        <!-- Cardboard NFC headset detection activity -->
        <activity
            android:name="com.google.cardboard.sdk.HeadsetDetectionActivity"
            android:exported="true"
            android:label="Headset Detection">
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:scheme="cardboard"/>
            </intent-filter>
        </activity>

    </application>
</manifest>
